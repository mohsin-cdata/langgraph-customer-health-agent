# CData Connect AI
CDATA_EMAIL=your-email@company.com
CDATA_PAT=your-personal-access-token

# LLM Provider (openai, anthropic, google, ollama)
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o
OPENAI_API_KEY=sk-proj-...

# Optional: force a specific catalog for demos
# CDATA_CATALOG=MCP_Apps_Demo

# Optional: schema cache TTL in seconds (default: 86400 = 24h)
# SCHEMA_CACHE_TTL=86400

# Optional: log level (INFO or DEBUG for verbose)
# LOG_LEVEL=INFO
